# Pending CLAUDE.md changes (human review)

The capture protocol forbids editing any `CLAUDE.md` directly (it is always loaded into context). Proposed CLAUDE.md edits land here. Review, apply by hand, then delete the entry.

Format: `- [ ] <unit>/CLAUDE.md — <proposed change> (origin: <session / date>)`

---

_(none)_
